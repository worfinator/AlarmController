{
  "name": "alarm_monitor",
  "version": "1.0.0",
  "description": "Alarm monitor application for pi_alarm",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Mark Baldwin",
  "license": "ISC",
  "dependencies": {
    "aws-iot-device-sdk": "^2.2.1",
    "config": "^3.0.1"
  }
}
